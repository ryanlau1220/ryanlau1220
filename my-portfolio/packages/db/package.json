{
  "name": "@portfolio/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "pnpm exec tsx seed.ts",
    "db:studio": "DB_URL=$(find ../../apps/portfolio/.wrangler -name '*.sqlite' ! -name 'metadata.sqlite' 2>/dev/null | head -1) && test -n \"$DB_URL\" && drizzle-kit studio || echo 'No SQLite database found. Run `pnpm run dev` in portfolio-app first.'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.2",
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^6.0.2"
  }
}
